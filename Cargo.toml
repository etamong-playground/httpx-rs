[package]
name = "etamong-httpx"
version = "0.1.0"
edition = "2021"
description = "Report-once HTTP error handling for Rust — a language-portable error contract with client ref codes."
license = "MIT OR Apache-2.0"
repository = "https://github.com/etamong-playground/httpx-rs"

[dependencies]
serde_json = "1"

[dev-dependencies]
serde_json = "1"
